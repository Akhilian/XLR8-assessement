import React, { PureComponent } from "react";
import PropTypes from 'prop-types';
import Capability from "../api/Capabilities";
import Respondent from "../api/Repondent";
import API from "../api";

export default class CapabilityResult extends PureComponent {
    render() {
        return (<div className="capability-results">
            <div className="">
                TOTO
            </div>
        </div>)
    }
}

CapabilityResult.propTypes = {
};
